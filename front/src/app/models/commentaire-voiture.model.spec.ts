import { CommentaireVoiture } from './commentaire-voiture.model';

describe('CommentaireVoiture', () => {
  it('should create an instance', () => {
    expect(new CommentaireVoiture()).toBeTruthy();
  });
});
